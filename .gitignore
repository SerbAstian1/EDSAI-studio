node_modules/
dist/
*.tsbuildinfo

# Secrets — never commit. This repo is public.
.env
.env.*
!.env.example

# Run records hold brief content and client material.
*.db
*.db-journal
*.sqlite
data/runs/

# Local dev server state — database and uploaded files from `edsai serve`
# run with no explicit --db/--assets, defaulting under the repo.
.edsai/

# Design references dropped in for a session, not part of the source.
ui ref/

# Exemplar imagery is third-party client work (Disan's Footwear).
# The data model marks it "local library, never redistributed".
# Kept out of this repo while it is public — see docs/notes/gaps.md.
corpus/assets/exemplars/

# Bundled plugin output — rebuilt with `pnpm --filter @edsai/figma bundle`.
packages/figma/plugin/dist/

# Studio build output — rebuilt with `pnpm --filter @edsai/studio build`.
packages/studio/dist/
packages/studio/dist-preview/
measurements.json

# Rust build output from the desktop shell that is no longer built.
packages/desktop/src-tauri/target/
