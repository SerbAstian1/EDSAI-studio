{
  "name": "@edsai/executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "bin": { "edsai-execute": "./dist/bin/execute.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@edsai/engine": "workspace:*",
    "@edsai/instruments": "workspace:*",
    "@edsai/rubric": "workspace:*",
    "openai": "^7.23.0"
  }
}
